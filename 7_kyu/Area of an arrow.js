function arrowArea(a, b) {
    const triangleOneArea = 0.5 * (a / 2) * (b / 2)
    const triangleTwoArea = 0.5 * (a / 2) * (b / 2)
    return triangleOneArea + triangleTwoArea
}

arrowArea(4, 2)
arrowArea(7, 6)